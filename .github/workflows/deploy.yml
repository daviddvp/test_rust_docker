name: Deploy
on: workflow_call
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Chekout repository
        uses: actions/checkout@v4
      - name: List files
        run: ls -la
      - name: Deploy
        run: echo "Deploying Rust App"
      
